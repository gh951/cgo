# 새 기능 붙이는 법 (CGO-FULI v5)

한 기능 = 파일 하나(`기능.js`) + `index.html` 안 페이지 하나.
아래 다섯 자리에 등록하지 않으면 반드시 되돌아온다.

| 자리 | 어디 | 무엇 |
|---|---|---|
| ① | perm.js `CAM_PAGES` | 그 페이지 id (카메라 쓰는 기능만) |
| ② | perm.js 취소 목록 | 그 기능의 **취소** 함수 이름 (결과 만드는 종료 함수 금지) |
| ③ | index.html `cgoHome()` 팝업 목록 | 그 기능 팝업 id 전부 |
| ④ | 기능 파일 맨 아래 `cgoRepaintOn(fn)` | 언어 바뀔 때 다시 칠하기 (감시자를 새로 만들지 않는다) |
| ⑤ | 검사 시작부 `_cgoStopAllCams()` | 앞 기능 카메라 놓아주기 |

## 글자와 번역

- 마크업 글자는 `data-k="번호"` 로만 둔다. 글자를 마크업에 두지 않는다.
- JS가 만드는 글자는 `_기능K(번호,'한국어')` 로 감싼다. 사전이 없어도 화면이 비지 않는다.
- 번호 사전은 index.html `<script id="dash6">` 한 곳. 언어 20개: ko en ja zh zh_HK ru es fr de pt it nl vi th id ms tl tr hi ar
- 순서: 한국어·영어 → 검증 → 러시아어 → 검증 → 나머지 4~5개씩.
- 마무리 검사: 빠짐 0 · 한국어 잔류 0 · 문법 오류 0.

## 기능별 번호 자리 (겹치지 않게)

| 번호대 | 기능 |
|---|---|
| 12000~12358 | AR 메이크업 (armk.js) |
| 12500~12637 | 색 이름 낱말 사전 (armk.js) |
| 12800~12828 | 피부 탄력 분석 입구 팝업 (svi.js) |

## 손대지 않는 것

- 구 CGO에서 옮긴 측정 알고리즘의 숫자·수식
- 완성된 기능 (요청 없이 건드리지 않는다)
- 색·팝업 규칙: 밝은 민트 · 전체화면 팝업 top:0 + 안쪽 여백 56px

# CGO-FULI 재구축 계획

작성 2026-08-09 · 1창 완료 시점

---

## 왜 다시 짓는가

이전 앱은 한국어로 만들고 나중에 번역을 얹었습니다.
그래서 언어를 늘릴 때마다 **화면의 글자를 하나하나 읽어 바꾸는 일**이 생겼고,
그것이 느림·잔상·혼용의 원인이었습니다.

이번에는 **영어를 본체로 짓습니다.** 번역은 자리마다 붙은 번호에 값을 쓰는 일이 됩니다.
글자를 읽지 않으므로, 언어가 21개로 늘어도 걸리는 시간은 그대로입니다.

---

## 네 가지 원칙

| | 원칙 | 뜻 |
|---|---|---|
| ① | **영어가 본체** | HTML 글자는 전부 영어. 영어 사용자는 번역 동작이 0 |
| ② | **번역은 번호로** | `data-k="번호"` 자리에 팩 값을 쓴다. 검사기 없음 |
| ③ | **보이는 것만** | 속 페이지는 `<template>`에 잠들어 있다가 누를 때 붙는다 |
| ④ | **사전은 최소** | 화면 자리는 전부 번호. 사전은 결과 텍스트 전용 |

③이 파트너님 특허 구조입니다. 39개 기능을 다 만들어도 브라우저가 그리는 것은
**지금 보이는 한 페이지뿐**입니다.

---

## 창 나누기 (총 5창)

한 창에서 다 만들 수 없으므로 기능 묶음으로 나눕니다.
창이 바뀌어도 **번호 구간이 정해져 있어 부딪히지 않습니다.**

| 창 | 담당 | 번호 구간 | 상태 |
|---|---|---|---|
| **1창** | 뼈대 · 입구 · 대시보드 · 번역 구조 | 1~499 | ✅ 완료 |
| **2창** | 건강 (rPPG · 인지 · 두피 · IQ · 눈 · 수면) | 600~1099 | 대기 |
| **3창** | 역학 (사주 · 타로 · 리포트 · 궁합 · 작명) | 1100~1599 | 대기 |
| **4창** | 생활 (음악 · 음식 · 쇼핑 · 여행 · 주식) | 1600~2099 | 대기 |
| **5창** | 마이페이지 · 결제 · 보안 · 마감 | 2100~2499 | 대기 |

### 번호 구간 상세

```
1~99      공통 (머리 · 꼬리 · 버튼 · 안내)
100~499   대시보드 (배너 · 카드 · 39타일)
500~599   여행         ← 견본으로 먼저 만듦
600~1099  건강 8기능
1100~1599 역학 12기능
1600~2099 생활 11기능
2100~2499 마이페이지 · 결제
```

기능 하나당 **50~100자리**를 씁니다. 여유 있게 잡았습니다.

---

## 1창에서 만든 것

```
입구        보안 코드 → 언어 선택 → 대시보드
대시보드    배너(4장 회전) · 실시간 3카드 · 5개 접이단 · 39개 타일
여행 페이지 견본 (민트 팝업 + 밝은 페이지)
번역        5개 언어 (한 · 영 · 일 · 중 · 번체 · 러) + 15개 언어 자리
```

### 측정된 속도

```
언어 전환    0.4ms
페이지 열기  1~2ms
전체 부팅    측정 필요
```

---

## 각 창의 작업 방법

**새 창을 열 때 전달할 것:**

```
1. v2 폴더 통째로 (index.html + lang/)
2. "N창 시작 — 담당 기능은 A·B·C, 번호는 X~Y번"
3. 이 문서
```

**새 창이 할 일:**

```
① 담당 기능의 <template> 작성 (영어로)
② data-k 번호 붙이기 (배정된 구간 안에서)
③ lang/*.json 6개 언어에 그 번호 채우기
④ 대시보드 타일과 연결
⑤ 속도 측정 후 파일 전달
```

**지켜야 할 것:**

- 번호 구간 밖으로 나가지 않기
- 영어를 HTML에 박기 (한국어로 만들고 번역하지 않기)
- `<template>` 안에 두기 (처음부터 화면에 그리지 않기)
- 새 문구는 **6개 언어를 함께** 채우기 (나중에 한 언어만 빠지는 사고 방지)

---

## 번역 늘리는 법

15개 언어가 자리만 잡혀 있습니다. 켜는 법:

```
1. lang/english.json 을 통째로 번역기에 넣는다
2. 결과를 lang/<코드>.json 으로 저장 (번호는 그대로)
3. index.html 언어 목록에서 마지막 값을 0 → 1
   예) ["es","Español","ES",0] → ["es","Español","ES",1]
```

아랍어는 오른쪽에서 왼쪽으로 흐르므로 **화면 배치 작업이 따로** 필요합니다.
번역과는 다른 일이라 마지막에 잡는 것이 좋습니다.

---

## 마감 순서

```
1창 ✅ → 2창 → 3창 → 4창 → 5창
                              ↓
                        전체 합치기
                              ↓
                        21개 언어 번역
                              ↓
                        아랍어 RTL
                              ↓
                        속도 최종 측정
```

---

## 주의할 점

**번호가 겹치면 화면이 뒤섞입니다.**
1창에서 여행 페이지를 200번대로 만들었다가 대시보드 타일과 21개가 부딪혔습니다.
500번대로 옮겨 해결했지만, 창이 나뉘면 이 사고가 더 잦습니다.
**배정된 구간을 반드시 지켜야 합니다.**

**한 언어만 채우면 나중에 그 언어가 튑니다.**
이전 앱에서 러시아어만 채우다 일본어·중국어에 같은 구멍이 생겼습니다.
새 문구는 **항상 6개 언어를 함께** 채웁니다.

**팩을 못 읽으면 영어로 돌아갑니다.**
반쪽 번역보다 통짜 영어가 낫다는 원칙입니다. 이미 그렇게 만들어져 있습니다.
